import React, { useRef } from 'react'
import { fetchLink } from '../../Functions/fetchLink'

function AnimationSeasonal() {
    const seasonalRef = useRef(undefined)
    const imglinks = [fetchLink('images/fruits'), fetchLink('images/oranges'), fetchLink('images/banana.')]
  return (
    <div>
      
    </div>
  )
}

export default AnimationSeasonal
